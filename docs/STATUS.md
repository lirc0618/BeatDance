# 当前实施状态 v0.3

## 已完成

- 产品已从“整舞 AI 分析”重构为“Feed 动作卡点搜索”；
- 微信小程序和 H5 已实现 Feed 暂停式入口；
- 支持手部、脚步、拍点、自动判断四种关注点；
- 诊断结果包含一个主导卡点、搜索词和 Top-3 拆解内容；
- 保留镜像校正、骨架归一化、DTW、节奏/路线/幅度诊断和二练验证；
- 已打包一条真实开放动作视频，而不再只有下载脚本；
- 真实视频解码、时长校验、随机帧读取、FFmpeg 归一化全部通过；
- 后端 8 项算法与召回单元测试通过。

## 尚未完成

- 真实视频的 MediaPipe 33 点端到端测试；
- 三个预设动作的参考视频注册；
- 正常、节奏、路线、幅度真人样本标定；
- 豆包线上请求验证；
- 微信真机与公网部署验收。

## 当前核心阻塞

不是“没有任何视频”，而是缺少能用于诊断阈值的同人、同机位标准/错误样本。开放视频可验证工程链路，但不能证明三类错误识别准确率。

## 下一步顺序

1. 在 Python 3.11 Docker 环境运行 MediaPipe 真实视频测试；
2. 用同一开放样例注册参考，再用同文件分析，先验证 `aligned`；
3. 录制并注册 `arm_wave` 真人参考视频；
4. 为 `arm_wave` 各录 2 条正常、节奏、路线、幅度样本；
5. 标定阈值并冻结第一个动作；
6. 再扩展另外两个动作；
7. 最后接豆包、微信真机和公网部署。
