# 本地完整流程验收报告

验收日期：2026-07-25
环境：macOS arm64、Python 3.11、MediaPipe 0.10.21、FFmpeg 8.1、Chromium 151

## 结论

三个内置 Feed 的完整自动化闭环已通过：播放、暂停解释、首练诊断、Top-3
多样化搜索、二练改善验证和结果删除。动态第 4 条另做了真实 CLI 导入/替换、
重启与单次暂停分析实测，未把它表述为二练自动化覆盖。

带暂停时间的分析直接使用 Feed 前后 1.5 秒真实片段；无暂停时间的兼容流程
使用同源、同动作参考，不再使用语义错配参考。开放样例仍只能证明工程链路，
不能作为诊断阈值准确率证据。产品阶段仍需真人同机位素材标定。

## 自动验收结果

运行命令：

```bash
make test
make lint
make accept
```

结果：

- 后端与脚本回归测试：22/22 通过；
- 动态 Feed：模块级新增/替换/失败回滚，以及带令牌的真实 multipart HTTP
  导入 → 暂停解释 → 单次分析通过；
- H5：用网络响应注入新动作的浏览器测试验证 5 秒轮询刷新；
- Ruff：通过；
- 三个动作参考状态：3/3；
- 三条 Feed：真实暂停后解锁入口，服务端读取前后 1.5 秒画面、实际时长和动作阶段；
- 暂停解释：按背面、慢速、局部顺序返回三种拆解；
- 3 秒以下和 8 秒以上视频：HTTP 422，可读时长提示；
- 无人黑屏视频：HTTP 422，返回单人全身入镜提示；
- 非法参考更新：HTTP 422，原参考保持可用；
- 三个动作：均返回一个主诊断、三项指标、三种不同 `view_type`、可点击抖音搜索和 JPEG 对比图；
- 豆包未配置：规则诊断正常完成；
- 错误首练 → 正确二练：按首练主卡点指标返回 `improved=true` 且改善比例大于 5%；
- 镜像输入：自动校正并返回镜像提示；
- 结果删除：JSON 与对比图均变为 HTTP 404；
- H5 Chromium：播放 Feed → 暂停 18.0 秒 → 阶段解释 → 三种拆解 → 错误首练 → 正确二练 → 改善提示通过；
- 远程 `?api=` 配置：对比图正确使用 API 服务域名；
- 自动验收产生的分析记录和对比图：脚本退出时自动清理。

## 本次人工实测

- 使用 CLI 新增 `two_step_demo`，再以同一个 ID 替换，服务返回
  `created=false`；
- 服务重启后健康检查为 4/4，动态动作仍可播放且参考可用；
- 在动态动作 3.0 秒暂停，返回三种拆解，并完成一次使用暂停上下文的分析；
- 动态动作的二练改善链路未单独重复；该链路由五个内置动作的自动验收覆盖。

## 稳定性与性能

同一输入每个动作连续运行 5 次，主诊断签名保持一致：

| 动作 | 平均耗时 | 最慢耗时 | 25 秒目标 |
|---|---:|---:|---|
| `groove_step` | 0.85s | 0.85s | 通过 |
| `arm_wave` | 1.29s | 1.30s | 通过 |
| `cross_step` | 0.90s | 0.91s | 通过 |

## 尚需外部条件

- 三个动作的团队真人标准参考视频；
- 正常、节奏、路线、幅度真人同机位样本与 Top-1 准确率标定；
- 多人画面的可靠识别与提示；
- 豆包真实接入点的线上调用/超时验证；
- 微信开发者工具、两台手机和微信内置浏览器验收；
- Docker 持久卷结构已配置；由于本机无 Docker，容器重启持久性仍待有 Docker
  的环境实测；
- 公网 HTTPS 域名、体验版二维码和完整录屏。
