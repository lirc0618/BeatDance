# v1 → v2 重构说明

## 保留

- 姿态提取、归一化、镜像、DTW；
- 三类差异诊断；
- 二练改善；
- 部署和客户端骨架。

## 替换

- “选择预设动作” → “在 Feed 中定格动作片段”；
- “上传后给 AI 分析” → “先表达卡点，再上传”；
- “一个针对性教程” → “状态搜索词 + 三种不同拆法”；
- “动作学习工具” → “短视频视觉搜索闭环”。

## 删除叙事

- 整舞评价；
- 练舞管理；
- 专业舞蹈评分；
- 长期训练统计。

## 代码兼容

- `/actions` 路径保留，但返回 Feed 元数据；
- `/analyze` 新增可选 `focus`，旧客户端不传时默认为 `auto`；
- `diagnosis.tutorial` 保留为首条结果，新增 `search_results`；
- 原 v1 参考视频和诊断参数可直接复用。
